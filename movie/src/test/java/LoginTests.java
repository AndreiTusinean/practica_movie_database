public class LoginTests {

    /*@Test
    public void checkLoginUI() {
        LoginUI loginUI = new LoginUI(0);
        String name = "wick", pass = "pass";
        loginUI.setName(name);
        loginUI.setPass(pass);
        //assertEquals(loginUI.getName(), name);
        //assertEquals(loginUI.getPass(), pass);
    }

    @Test
    public void checkAdminUI() {
        AdminUI adminUI = new AdminUI();
        String name = "wick", pass = "pass", type = "admin";
        int id = 1, location = 2;

        adminUI.setId(id);
        adminUI.setLocation(location);
        adminUI.setName(name);
        adminUI.setPass(pass);
        adminUI.setType(type);

        assertEquals(adminUI.getId(), id);
        assertEquals(adminUI.getLocation(), location);
        assertEquals(adminUI.getName(), name);
        assertEquals(adminUI.getPass(), pass);
        assertEquals(adminUI.getType(), type);
    }

    @Test
    public void checkEmployeeUI() {
        EmployeeUI employeeUI = new EmployeeUI();
        String name = "cheese";
        int id = 1, amount = 2, price = 3, location = 4;

        employeeUI.setName(name);
        employeeUI.setId(id);
        employeeUI.setAmount(amount);
        employeeUI.setPrice(price);
        employeeUI.setLocation(location);

        assertEquals(employeeUI.getName(), name);
        assertEquals(employeeUI.getId(), id);
        assertEquals(employeeUI.getAmount(), amount);
        assertEquals(employeeUI.getPrice(), price);
        assertEquals(employeeUI.getLocation(), location);

    }

    @Test
    public void testLogin() throws IOException {
        Connection c = mock(Connection.class);
        LoginUI lu = mock(LoginUI.class);
        when(lu.getName()).thenReturn("keanu");
        when(lu.getPass()).thenReturn("123");
        LoginDAO.checkLoginDAO("keanu", "123");
        verify(lu).getName();
        verify(lu).getPass();
        //verify(c.sendMessageToServer("loginCheck,keanu,123"));
    }*/
}


